# Virtual environment
.venv/
venv/

# Python caches
__pycache__/
*.pyc

# User uploads (runtime)
uploads/*
!uploads/.gitkeep

# Model weights are too large for GitHub (>100 MB). Provide separately.
model/model.safetensors
